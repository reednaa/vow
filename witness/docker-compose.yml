services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vow
      POSTGRES_PASSWORD: vow
      POSTGRES_DB: vow_witness
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
